from django.apps import AppConfig

class JobManagementConfig(AppConfig):
    '''Configuration for the Job Management app'''
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'job_management'
